# Copy to .env.local for local dev (optional)
# Leave a variable unset rather than empty: NEXT_PUBLIC_SITE_URL= breaks metadata if the value is blank.
NEXT_PUBLIC_DISCORD_INVITE_URL=
NEXT_PUBLIC_GITHUB_REPO=https://github.com/your-org/velocitydrop
NEXT_PUBLIC_SITE_URL=http://localhost:3000
